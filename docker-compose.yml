services:
  backend:
    build: ./backend
    environment:
      SENSOR_SIMULATOR_URL: http://sensor:8001
    ports:
      - "8000:8000"

  sensor:
    build: ./sensor
    environment:
      BACKEND_URL: http://backend:8000
    depends_on:
      - backend
    ports:
      - "8001:8001"
